// src/features/jobs/components/FilterSidebar/types.ts

export type FilterForm = {
    query: string;
    location: string;
    experienceLevel: string;
    applyType: string;
    sort: string;
    minSalary: number;
    maxSalary: number;
    skills: string[];
  }
  